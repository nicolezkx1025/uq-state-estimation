# NOTES — 设计取舍与开放问题

这份笔记记录实验背后的判断,以及我目前**还没有解决**的问题。写给未来的自己,也写给愿意指出错误的人。

## 为什么选这个题目

传感器退化(雾、雨、GPS 失效、异常外点)在真实系统里是常态,而不是例外。工程上通常的做法是
"让滤波器更鲁棒"——但那解决的是**精度**。真正决定系统能否安全部署的是另一件事:
**它报告的不确定性是否可信**。

一个"精度不错但过度自信"的滤波器,在低容错场景里比一个"精度略差但诚实"的滤波器更危险:
前者会让下游以为可以行动,后者会自动降级为谨慎策略。这个差别在干净数据集上**完全看不出来**,
这也是本仓库存在的原因——把差别放到台面上量化。

## 关键设计取舍

1. **两个滤波器只差一处**:`static` 与 `adaptive` 共用运动模型、数据、初始化,唯一区别是
   退化时是否放大观测噪声。这样任何指标差异都只能归因于"协方差口径",而不是别的因素。
2. **指标用覆盖率与 NEES,而不只是 RMSE**。RMSE 衡量精度;覆盖率和 NEES 衡量"宣称的置信度是否兑现"。
   一个只报 RMSE 的实验无法回答本项目的问题。
3. **退化指示量故意设为"已知"(oracle)**。这是为了让第一版实验干净:先确认"如实建模退化能修好校准",
   再去解决"如何检测退化"。反过来做会让两个问题混在一起,结论无法归因。
4. **不依赖 matplotlib**。图形用极简 SVG 手写生成,目的是让 `pip install numpy` 之后**一条命令即可复现**,
   避免环境问题淹没结论。

## 尚未解决 / 存疑的地方

- **退化检测本身是难点**。现实中没有人告诉你"现在相机退化了"。而且检测器自身也有校准问题:
  漏检会重新引入过度自信,误检会过度保守。这两类错误如何量化,是本项目下一步的核心。
- **雾不只增大方差,还会引入偏差**。当前模型只放大了噪声并加入外点;真实的雾会造成系统性偏移。
  偏差不会被协方差吸收,会导致**随时间累积的失配**——覆盖率会随运行时长单调恶化。这值得单独做一组实验。
- **NEES 在非高斯误差下不严谨**。外点会破坏高斯假设,此时 NEES 的绝对值只能作参考,
  覆盖率与可靠性曲线更稳健。`both` 模式下 NEES=8.26 说明该模式仍有残余失配,
  我倾向于认为问题在偏差/门控策略,而非协方差公式本身。
- **门控阈值是手工选的**(χ² 分位)。没有做消融,不确定它贡献了多少改善。
- **`both` 模式下的结论需要更强的统计支撑**。目前 20 个种子,覆盖率的标准差未报告;
  做论文级结论前应给出置信区间。
- **合成数据不能替代真实退化**。下一步希望换成公开数据集或一台真机(例如小型移动平台 + 相机),
  让退化来自物理世界,而不是我写的噪声模型。

## 长期方向

把"不确定性是否可信"从**事后指标**变成**在线可用的信号**:系统在运行时自己判断
"我现在的状态估计还能不能支撑行动"。这既是可信自主系统的工程前提,
也与我更长期关心的问题相连——智能系统如何表示、观测并信任自己的内部状态。
